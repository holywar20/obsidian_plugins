{
	"name": "filename-search",
	"version": "1.0.0",
	"description": "Search the vault by filename from a dedicated sidebar panel.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"esbuild": "^0.21.0",
		"obsidian": "latest",
		"typescript": "^5.4.0"
	}
}
